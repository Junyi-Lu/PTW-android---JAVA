<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp3350.ptw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">


        <activity
            android:name=".presentation.HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.PTWActivity" />
        <activity android:name=".presentation.RecommendActivity" />
        <activity android:name=".presentation.WatchingActivity" />
        <activity android:name=".presentation.WatchedActivity" />
        <activity android:name=".presentation.ContentActivity" />
        <activity
            android:name=".presentation.SearchActivity"
            android:launchMode="singleTop"></activity>

    </application>

</manifest>